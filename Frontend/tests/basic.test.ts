import { describe, expect, test } from 'vitest';

describe('Baheria Cars Frontend', () => {
    test('basic calculation should work', () => {
        const result = 2 + 3;

        expect(result).toBe(5);
    });
});